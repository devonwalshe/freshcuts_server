= form_with(model: post_category, local: true) do |form|
  - if post_category.errors.any?
    #error_explanation
      %h2
        = pluralize(post_category.errors.count, "error")
        prohibited this post_category from being saved:
      %ul
        - post_category.errors.full_messages.each do |message|
          %li= message
  .actions
    = form.submit
